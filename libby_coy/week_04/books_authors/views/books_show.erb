<h1><%= @book['name'] %></h1>
<h2>Book: <em><%= @book['name'] %></em></h2>
<% if @book.author %>
  <h2>Author:
    <em>
      <a href="/books/<%= @book.author.id %>">
        <%= @book.author.name %>
      </a>
    </em>
  </h2>
<% end %>

<% unless @book['image'].nil? %>
  <img src="<%= @book['image'] %>" alt="<%= @book['name'] %>" class="feature">
<% end %>

<h3><%= @book['author_id'] %></h3>

<div class="controls">
  <a href="/books/<%= @book['id'] %>/delete">Delete</a>
  <a href="/books/<%= @book['id'] %>/edit">Edit</a>
</div>
