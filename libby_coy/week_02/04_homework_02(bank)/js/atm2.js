// Keep track of the checking and savings balances somewhere

//    number = input of ;

  $('#checking-balance')

// replace #checking-balance with 

    var valueChecking = function(number) {
      var checkBalance = 0;
      var checkChecking = checkBalance + number;
      console.log(checkChecking);
    }

    valueChecking(10);

//    number2 = input of ;

    var valueSavings = function(number2) {
      var savingsBalance = 0;
      var checkSavings = savingsBalance + number2;
      console.log(checkSavings);
    }

    valueSavings(20);

  //   var checkForWinner = function () {
  //   var result = $('.wheel').text();
  // }
